<!DOCTYPE html>
<html lang="id" style="background-color: #0a0b0d; color-scheme: dark;">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0">
  <title><%= typeof title !== 'undefined' ? title : 'AlexCloud | Platform Cloud Gaming Premium #1 Indonesia' %></title>
  <meta name="description" content="AlexCloud - Layanan Cloud Gaming Premium & Terbaik di Indonesia. Mainkan ratusan game PC/Console AAA terbaru (EA FC 26, GTA VI, dll) langsung di HP, Laptop, atau Smart TV tanpa perlu beli PC mahal. Server lokal anti lag, harga mulai Rp 40.000/minggu. Coba sekarang!">
  <meta name="keywords" content="cloud gaming indonesia, cloud gaming murah, main game AAA di hp, game cloud server indonesia, sewa cloud gaming pc, alexcloud, alexcloud gaming">
  <meta name="robots" content="index, follow, max-snippet:-1, max-image-preview:large">
  <meta name="author" content="AlexCloud">
  <link rel="canonical" href="<%= typeof canonicalUrl !== 'undefined' ? canonicalUrl : 'https://alexcloud.my.id/' %>">
  <meta name="theme-color" content="#0a0b0d">

  <!-- Open Graph / Facebook (Tampilan premium saat di-share di WhatsApp/Sosmed) -->
  <meta property="og:type" content="website">
  <meta property="og:url" content="<%= typeof canonicalUrl !== 'undefined' ? canonicalUrl : 'https://alexcloud.my.id/' %>">
  <meta property="og:site_name" content="AlexCloud">
  <meta property="og:title" content="AlexCloud | Platform Cloud Gaming Premium #1 Indonesia">
  <meta property="og:description" content="Main game AAA terbaru langsung di HP, laptop, atau TV tanpa PC mahal. Mulai Rp 40.000/minggu dengan server lokal ping rendah!">
  <meta property="og:image" content="https://alexcloud.my.id/images/og-cover.png">
  <meta property="og:image:width" content="1200">
  <meta property="og:image:height" content="630">
  
  <!-- Twitter -->
  <meta property="twitter:card" content="summary_large_image">
  <meta property="twitter:url" content="<%= typeof canonicalUrl !== 'undefined' ? canonicalUrl : 'https://alexcloud.my.id/' %>">
  <meta property="twitter:title" content="AlexCloud | Platform Cloud Gaming Premium #1 Indonesia">
  <meta property="twitter:description" content="Main game AAA terbaru langsung di HP, laptop, atau TV tanpa PC mahal. Mulai Rp 40.000/minggu dengan server lokal ping rendah!">
  <meta property="twitter:image" content="https://alexcloud.my.id/images/og-cover.png">

  <!-- Google Schema.org Structured Data -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Organization",
    "name": "AlexCloud",
    "alternateName": ["Alex Cloud", "AlexCloud Gaming"],
    "url": "https://alexcloud.my.id/",
    "logo": "https://alexcloud.my.id/favicon.svg",
    "description": "Platform Cloud Gaming Premium #1 Indonesia",
    "contactPoint": {
      "@type": "ContactPoint",
      "telephone": "+62-858-1007-3341",
      "contactType": "customer service",
      "areaServed": "ID",
      "availableLanguage": "Indonesian"
    },
    "sameAs": [
      "https://wa.me/6285810073341",
      "https://www.tiktok.com/@bentleysaja_",
      "https://www.instagram.com/beentley16",
      "https://t.me/bentley160"
    ]
  }
  </script>
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "WebSite",
    "name": "AlexCloud",
    "alternateName": ["Alex Cloud", "AlexCloud Gaming"],
    "url": "https://alexcloud.my.id/"
  }
  </script>

  <!-- Favicon & App Icons -->
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
  <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon">
  <link rel="apple-touch-icon" href="/favicon.svg">
  <link rel="manifest" href="/manifest.json">

  <!-- DNS prefetch for external image CDNs -->
  <link rel="dns-prefetch" href="https://shared.fastly.steamstatic.com">
  <link rel="dns-prefetch" href="https://api.dicebear.com">
  <!-- Poppins display font -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <!-- Preload self-hosted fonts (text fonts arrive before first paint → no swap reflow / CLS) -->
  <link rel="preload" href="/fonts/inter-latin.woff2" as="font" type="font/woff2" crossorigin>
  <link rel="preload" href="/fonts/jakarta-latin.woff2" as="font" type="font/woff2" crossorigin>
  <!-- Preload subset icon font (above-the-fold navbar/hero icons) -->
  <link rel="preload" href="/fonts/fa-solid-900.woff2" as="font" type="font/woff2" crossorigin>

  <!-- ═══ CRITICAL CSS: Inlined for instant first paint ═══ -->
  <style>
    :root{--bg-primary:#0a0b0d;--bg-secondary:#0f1013;--bg-card:#131418;--bg-card-hover:#1a1c22;--bg-elevated:#20222a;--bg-surface:#16171c;--accent:#fbbf24;--accent-hover:#fcd34d;--accent-cyan:#f59e0b;--accent-soft:rgba(251,191,36,0.12);--accent-line:rgba(251,191,36,0.32);--accent-blue:#fbbf24;--accent-purple:#d97706;--accent-orange:#fbbf24;--accent-secondary:#f59e0b;--accent-indigo:#fbbf24;--accent-violet:#d97706;--accent-green:#34d399;--text-primary:#ededf0;--text-secondary:#9aa0aa;--text-muted:#696e78;--border:rgba(255,255,255,0.07);--border-hover:rgba(255,255,255,0.16);--border-glass:rgba(255,255,255,0.08);--glow-blue:0 8px 28px rgba(0,0,0,0.55);--glow-accent:0 10px 40px rgba(251,191,36,0.28);--shadow-card:0 1px 2px rgba(0,0,0,0.40),0 12px 32px rgba(0,0,0,0.34);--shadow-card-hover:0 26px 60px rgba(0,0,0,0.58),0 0 0 1px rgba(251,191,36,0.22);--gradient-main:linear-gradient(120deg,#fbbf24 0%,#f59e0b 100%);--gradient-warm:linear-gradient(120deg,#f59e0b 0%,#d97706 100%);--gradient-aurora:linear-gradient(120deg,#fcd34d 0%,#fbbf24 45%,#f59e0b 100%);--gradient-dark:linear-gradient(180deg,#0a0b0d 0%,#0f1013 100%);--gradient-glass:linear-gradient(135deg,rgba(255,255,255,0.05) 0%,rgba(255,255,255,0.012) 100%);--gradient-accent:linear-gradient(120deg,#fbbf24 0%,#f59e0b 100%);--glass-bg:rgba(16,17,21,0.66);--glass-blur:blur(18px) saturate(125%);--font-display:'Poppins','Plus Jakarta Sans','Inter','Segoe UI',sans-serif;--font-body:'Inter','Segoe UI',sans-serif;--font-ui:'Poppins','Inter','Segoe UI',sans-serif;--radius:12px;--radius-lg:16px;--radius-xl:22px;--radius-pill:40px;--transition:0.2s cubic-bezier(0.4,0,0.2,1);--transition-fast:0.12s cubic-bezier(0.4,0,0.2,1)}
    *,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
    html{scroll-behavior:smooth}
    body{font-family:var(--font-body);background:var(--bg-primary);color:var(--text-primary);min-height:100vh;overflow-x:hidden;line-height:1.6}
    .scroll-progress{display:none;position:fixed;top:0;left:0;width:0%;height:3px;background:var(--gradient-main);z-index:10001}
    .skip-link{position:absolute;top:-100%;left:50%;transform:translateX(-50%);background:var(--gradient-main);color:#fff;padding:0.75rem 1.5rem;border-radius:0 0 var(--radius) var(--radius);z-index:10000;font-weight:700;text-decoration:none;transition:top 0.3s ease}
    .skip-link:focus{top:0}
    .navbar{position:fixed;top:0;left:0;right:0;z-index:1000;padding:0.85rem 0;background:rgba(11,12,15,0.70);backdrop-filter:blur(20px) saturate(120%);-webkit-backdrop-filter:blur(20px) saturate(120%);border-bottom:1px solid var(--border);transition:var(--transition)}
    .navbar.scrolled{background:rgba(11,12,15,0.88);border-bottom-color:var(--border);box-shadow:0 1px 0 rgba(0,0,0,0.4),0 8px 30px rgba(0,0,0,0.5)}
    .nav-container{max-width:1280px;margin:0 auto;padding:0 2rem;display:flex;align-items:center;gap:2rem}
    .nav-logo{text-decoration:none;display:flex;align-items:center;gap:0.6rem;flex-shrink:0}
    .logo-text{font-family:var(--font-display);font-size:1.3rem;font-weight:700;color:var(--text-primary);letter-spacing:2px}
    .logo-accent{color:var(--accent-blue)}
    .nav-links{display:flex;align-items:center;gap:0.5rem;margin-left:auto;flex-wrap:wrap}
    .nav-link{color:var(--text-secondary);text-decoration:none;font-family:var(--font-ui);font-size:1rem;font-weight:500;padding:0.5rem 0.9rem;border-radius:8px;transition:var(--transition)}
    .btn-nav-cta{background:var(--gradient-main);color:#fff !important;text-decoration:none;font-family:var(--font-ui);font-size:0.95rem;font-weight:700;padding:0.5rem 1.4rem;border-radius:8px;transition:var(--transition)}
    .nav-toggle{display:none;flex-direction:column;justify-content:center;align-items:center;gap:5px;width:42px;height:42px;background:rgba(255,255,255,0.05);border:1px solid var(--border-glass);border-radius:12px;cursor:pointer;padding:0;margin-left:auto;transition:var(--transition)}
    .nav-toggle span{display:block;width:20px;height:2px;background:var(--text-primary);border-radius:2px}
    .hero{min-height:100vh;display:flex;align-items:center;position:relative;padding-top:110px;overflow:hidden}
    .page-hero{padding:6rem 0 2rem}body.has-announcement .page-hero{padding-top:8.5rem}
    .hero-bg{position:absolute;inset:0;background:radial-gradient(ellipse 60% 70% at 72% 40%,rgba(255, 255, 255,0.06) 0%,transparent 62%)}
    .hero-grid-overlay{position:absolute;inset:0;background-image:linear-gradient(rgba(255,255,255,0.015) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,0.015) 1px,transparent 1px);background-size:80px 80px}
    .hero-container{max-width:1280px;margin:0 auto;padding:2.5rem 2rem 4rem;position:relative;z-index:1;display:grid;grid-template-columns:1fr 1fr;gap:4rem;align-items:center}
    .hero-badge{display:inline-flex;align-items:center;gap:0.5rem;background:var(--accent-soft);border:1px solid var(--accent-line);color:var(--accent);padding:0.4rem 1rem;border-radius:50px;font-family:var(--font-ui);font-size:0.85rem;font-weight:600;letter-spacing:1px;text-transform:uppercase;margin-bottom:1.5rem}
    .hero-title{font-family:var(--font-display);font-size:clamp(2.5rem,5vw,4.5rem);font-weight:800;line-height:1.08;letter-spacing:-1.5px;margin-bottom:1.5rem;color:var(--text-primary)}
    .hero-title .accent{background:var(--gradient-main);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
    .hero-subtitle{font-size:1.15rem;color:var(--text-secondary);max-width:500px;margin-bottom:2.5rem;line-height:1.7}
    .hero-actions{display:flex;gap:1rem;flex-wrap:wrap}
    .btn-primary{display:inline-flex;align-items:center;gap:0.5rem;background:var(--accent);color:#fff;text-decoration:none;font-family:var(--font-ui);font-size:1rem;font-weight:700;padding:0.9rem 2.2rem;border-radius:var(--radius);transition:var(--transition);border:none;cursor:pointer;position:relative}
    .btn-secondary{display:inline-flex;align-items:center;gap:0.5rem;background:transparent;color:var(--text-primary);text-decoration:none;font-family:var(--font-ui);font-size:1rem;font-weight:600;padding:0.9rem 2.2rem;border-radius:var(--radius);border:1px solid var(--border);transition:var(--transition);cursor:pointer}
    .hero-stats{display:flex;gap:2rem;margin-top:2.5rem;padding-top:2rem;border-top:1px solid var(--border)}
    .stat-item{text-align:center}
    .stat-num{display:block;font-family:var(--font-display);font-size:1.8rem;font-weight:700;color:var(--accent-blue)}
    .stat-label{font-family:var(--font-ui);font-size:0.8rem;color:var(--text-muted);text-transform:uppercase;letter-spacing:1px}
    .hero-visual{position:relative;display:flex;align-items:center;justify-content:center}
    .nav-search-wrap{flex:1;max-width:320px}
    .nav-search-box{position:relative;display:flex;align-items:center}
    .nav-search-input{width:100%;padding:0.5rem 1rem 0.5rem 2.2rem;border-radius:8px;border:1px solid var(--border);background:rgba(255,255,255,0.04);color:var(--text-primary);font-family:var(--font-ui);font-size:0.9rem;outline:none;transition:var(--transition)}
    .nav-search-icon{position:absolute;left:0.75rem;color:var(--text-muted);font-size:0.85rem;pointer-events:none}
    .nav-user-btn{display:flex;align-items:center;gap:0.5rem;background:rgba(255,255,255,0.05);border:1px solid var(--border);color:var(--text-primary);padding:0.4rem 0.9rem 0.4rem 0.6rem;border-radius:8px;cursor:pointer;font-family:var(--font-ui);font-size:0.95rem;font-weight:500}
    .nav-user-menu{position:relative}
    .nav-avatar{width:28px;height:28px;border-radius:50%;object-fit:cover}
    .nav-avatar-placeholder{width:28px;height:28px;border-radius:50%;background:var(--gradient-main);display:flex;align-items:center;justify-content:center;font-size:0.8rem;font-weight:700;color:#fff}
    .testi-avatar-placeholder{width:44px;height:44px;border-radius:50%;background:var(--gradient-main);color:#fff;display:flex;align-items:center;justify-content:center;font-size:1.1rem;font-weight:700;border:2px solid var(--accent-blue);flex-shrink:0}
    .dropdown{position:relative;display:inline-block}
    .dropdown-content{display:none;position:absolute;background:rgba(19,20,24,0.95);backdrop-filter:blur(10px);min-width:180px;box-shadow:0 8px 30px rgba(0,0,0,0.5);border:1px solid var(--border);border-radius:10px;z-index:10002;top:100%;left:50%;transform:translateX(-50%);margin-top:0.5rem;overflow:hidden;padding:0.35rem 0}
    .dropdown:hover .dropdown-content{display:block}
    .dropdown-content a{color:var(--text-secondary);padding:0.6rem 1rem;text-decoration:none;display:flex;align-items:center;gap:0.5rem;font-family:var(--font-ui);font-size:0.88rem;transition:var(--transition)}
    .dropdown-content a:hover{background:rgba(255,255,255,0.06);color:#fbbf24}
    @media(max-width:768px){
      .dropdown-content{position:static !important;transform:none !important;box-shadow:none !important;background:transparent !important;border:none !important;display:block !important;padding-left:1.5rem !important}
      .dropdown-content a{padding:0.4rem 1rem !important;font-size:0.95rem !important}
    }
    @media(max-width:1024px){.hero-visual{display:none}.hero-badge{margin-left:auto;margin-right:auto}}
    @media(max-width:768px){
      .nav-container{gap:0.6rem;padding:0 1rem}
      .logo-text{font-size:1.1rem;letter-spacing:1px}
      .nav-search-wrap{display:flex;flex:1;max-width:200px}
      .nav-search-input{padding:0.4rem 0.6rem 0.4rem 1.9rem;font-size:0.8rem;border-radius:6px}
      .nav-search-icon{left:0.55rem;font-size:0.75rem}
      .nav-links{display:none}
      .nav-toggle{display:flex}
      .hero{min-height:auto;padding-top:85px;padding-bottom:1.5rem}
    }
  </style>

  <!-- ═══ Self-hosted fonts (inlined @font-face = discovered instantly, no extra request/origin) ═══ -->
  <style>
    @font-face{font-family:'Inter';font-style:normal;font-weight:100 900;font-display:swap;src:url(/fonts/inter-latin.woff2) format('woff2');unicode-range:U+0000-00FF,U+0131,U+0152-0153,U+02BB-02BC,U+02C6,U+02DA,U+02DC,U+0304,U+0308,U+0329,U+2000-206F,U+20AC,U+2122,U+2191,U+2193,U+2212,U+2215,U+FEFF,U+FFFD}
    @font-face{font-family:'Inter';font-style:normal;font-weight:100 900;font-display:swap;src:url(/fonts/inter-latin-ext.woff2) format('woff2');unicode-range:U+0100-02BA,U+02BD-02C5,U+02C7-02CC,U+02CE-02D7,U+02DD-02FF,U+0304,U+0308,U+0329,U+1D00-1DBF,U+1E00-1E9F,U+1EF2-1EFF,U+2020,U+20A0-20AB,U+20AD-20C0,U+2113,U+2C60-2C7F,U+A720-A7FF}
    @font-face{font-family:'Plus Jakarta Sans';font-style:normal;font-weight:200 800;font-display:swap;src:url(/fonts/jakarta-latin.woff2) format('woff2');unicode-range:U+0000-00FF,U+0131,U+0152-0153,U+02BB-02BC,U+02C6,U+02DA,U+02DC,U+0304,U+0308,U+0329,U+2000-206F,U+20AC,U+2122,U+2191,U+2193,U+2212,U+2215,U+FEFF,U+FFFD}
    @font-face{font-family:'Plus Jakarta Sans';font-style:normal;font-weight:200 800;font-display:swap;src:url(/fonts/jakarta-latin-ext.woff2) format('woff2');unicode-range:U+0100-02BA,U+02BD-02C5,U+02C7-02CC,U+02CE-02D7,U+02DD-02FF,U+0304,U+0308,U+0329,U+1D00-1DBF,U+1E00-1E9F,U+1EF2-1EFF,U+2020,U+20A0-20AB,U+20AD-20C0,U+2113,U+2C60-2C7F,U+A720-A7FF}
  </style>

  <!-- Main CSS: render-blocking on purpose — first paint uses final layout, so the
       desktop hero (hero-visual card, styled only in main.css) doesn't reflow when
       an async stylesheet applies. Same-origin ~14KB gzip; FCP cost is tiny, CLS→~0. -->
  <link href="/css/main.css?v=40.0" rel="stylesheet">

  <!-- FontAwesome: self-hosted subset (~11KB woff2 vs ~290KB full kit), load non-blocking -->
  <link rel="stylesheet" href="/css/fontawesome.min.css?v=1" media="print" onload="this.media='all'">
  <noscript><link rel="stylesheet" href="/css/fontawesome.min.css?v=1"></noscript>
  <!-- CSRF token + auto-injeksi ke form & fetch/XHR (lihat middleware/csrf.js) -->
  <meta name="csrf-token" content="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
  <script>
  (function () {
    var meta = document.querySelector('meta[name="csrf-token"]');
    var token = meta ? (meta.getAttribute('content') || '') : '';
    if (!token) return;
    function decorate(form) {
      if (!form || (form.getAttribute('method') || form.method || '').toUpperCase() === 'GET') return;
      var enc = (form.getAttribute('enctype') || form.enctype || '').toLowerCase();
      if (enc.indexOf('multipart') !== -1) {
        // Body multipart diparse oleh multer SETELAH middleware CSRF → kirim via query.
        if (form.action.indexOf('_csrf=') === -1) {
          form.action += (form.action.indexOf('?') === -1 ? '?' : '&') + '_csrf=' + encodeURIComponent(token);
        }
      } else if (!form.querySelector('input[name="_csrf"]')) {
        var i = document.createElement('input');
        i.type = 'hidden'; i.name = '_csrf'; i.value = token;
        form.appendChild(i);
      }
    }
    document.addEventListener('submit', function (e) {
      if (e.target && e.target.tagName === 'FORM') decorate(e.target);
    }, true);
    document.addEventListener('DOMContentLoaded', function () {
      var forms = document.querySelectorAll('form');
      for (var i = 0; i < forms.length; i++) decorate(forms[i]);
    });
    function mutating(m) { m = (m || 'GET').toUpperCase(); return m !== 'GET' && m !== 'HEAD' && m !== 'OPTIONS'; }
    function sameOrigin(url) {
      if (!url || url.indexOf('http') !== 0) return true; // relatif = same-origin
      return url.indexOf(window.location.origin) === 0;
    }
    if (window.fetch) {
      var origFetch = window.fetch;
      window.fetch = function (input, init) {
        init = init || {};
        var method = init.method || (typeof input === 'object' && input && input.method) || 'GET';
        var url = typeof input === 'string' ? input : (input && input.url) || '';
        if (mutating(method) && sameOrigin(url)) {
          var headers = new Headers(init.headers || (typeof input === 'object' && input ? input.headers : null) || {});
          if (!headers.has('x-csrf-token')) headers.set('x-csrf-token', token);
          init.headers = headers;
        }
        return origFetch.call(this, input, init);
      };
    }
    var origOpen = XMLHttpRequest.prototype.open;
    XMLHttpRequest.prototype.open = function (method) {
      this.__csrf = mutating(method);
      return origOpen.apply(this, arguments);
    };
    var origSend = XMLHttpRequest.prototype.send;
    XMLHttpRequest.prototype.send = function () {
      if (this.__csrf) { try { this.setRequestHeader('x-csrf-token', token); } catch (e) {} }
      return origSend.apply(this, arguments);
    };
  })();
  </script>
</head>
<body>
  <!-- Scroll Progress Indicator -->
  <div class="scroll-progress" id="scrollProgress"></div>

  <!-- Skip to main content (accessibility) -->
  <a href="#main-content" class="skip-link">Langsung ke konten utama</a>
  <nav class="navbar" id="navbar">
    <div class="nav-container">
      <a href="/?ref_=nav_logo" class="nav-logo">
        <img src="/images/logo-icon.png" class="logo-icon-img" alt="AlexCloud Logo" width="28" height="28" style="object-fit: contain; flex-shrink:0;">
        <span class="logo-text">ALEX<span class="logo-accent">CLOUD</span></span>
      </a>

      <!-- AI Smart Search -->
      <div class="nav-search-wrap" id="navSearchWrap">
        <div class="nav-search-box">
          <i class="fas fa-search nav-search-icon"></i>
          <input type="text" id="navSearchInput" class="nav-search-input" placeholder="Cari game...">
          <div class="search-results-dropdown" id="searchDropdown"></div>
        </div>
      </div>

            <div class="nav-links" id="navLinks">
        <% if (typeof user !== 'undefined' && user) { %>
          <!-- Mobile Profile Card (Header Drawer) -->
          <div class="mobile-user-card desktop-hidden">
            <div class="mobile-user-info">
              <% if (user.avatar) { %>
                <img src="<%= user.avatar %>" style="width:44px; height:44px; border-radius:50%; object-fit:cover; border:2px solid #fbbf24;" alt="<%= user.name %>">
              <% } else { %>
                <div class="mobile-avatar-placeholder"><%= user.name.charAt(0).toUpperCase() %></div>
              <% } %>
              <div>
                <div class="mobile-user-name">
                  <%= user.name.split(' ')[0] %>
                  <% if (user.isRoyal) { %>
                    <i class="fas fa-crown" style="color:#fbbf24; font-size:0.85rem;" title="VIP Member"></i>
                  <% } %>
                </div>
                <div class="mobile-user-balance"><i class="fas fa-wallet" aria-hidden="true"></i> Rp <%= (typeof balance !== 'undefined' ? balance : 0).toLocaleString('id-ID') %></div>
              </div>
            </div>
            <div class="mobile-user-actions">
              <div class="mobile-user-btn-row">
                <% if (user.role === 'admin') { %>
                  <a href="/admin?ref_=nav_mobile_admin" class="btn-mobile-admin"><i class="fas fa-shield-alt" aria-hidden="true"></i> Admin</a>
                <% } %>
                <a href="/wallet?ref_=nav_mobile_topup" class="btn-mobile-topup">+ Top Up</a>
              </div>
              <a href="/logout" class="btn-mobile-logout">Logout</a>
            </div>
          </div>
        <% } %>

        <a href="/?ref_=nav_home" class="nav-link"><span><i class="fas fa-home"></i> Beranda</span></a>
        <a href="/games?ref_=nav_games" class="nav-link"><span><i class="fas fa-gamepad"></i> Game Library</span> <span class="badge-hot">100+ GAME</span></a>
        <a href="/pricing?ref_=nav_pricing" class="nav-link"><span><i class="fas fa-tags"></i> Paket &amp; Harga</span></a>
        <a href="/testimonials?ref_=nav_testimonials" class="nav-link"><span><i class="fas fa-star"></i> Testimoni</span></a>
        <a href="/community?ref_=nav_community" class="nav-link"><span><i class="fas fa-comments"></i> Komunitas</span></a>
        <a href="/network-test?ref_=nav_speedtest" class="nav-link"><span><i class="fas fa-wifi"></i> Speed Test</span></a>
        <a href="/faq?ref_=nav_faq" class="nav-link"><span><i class="fas fa-question-circle"></i> FAQ</span></a>

        <% if (typeof user !== 'undefined' && user) { %>
          <% if (user.role === 'admin') { %>
            <div class="nav-section-divider desktop-hidden nav-admin-divider">ADMINISTRATOR</div>
            <a href="/admin?ref_=nav_admin" class="nav-link nav-admin-card desktop-hidden"><span><i class="fas fa-shield-alt" aria-hidden="true"></i> Admin Panel</span></a>
            <a href="/admin?ref_=nav_admin" class="nav-link mobile-hidden" style="color:#fbbf24; font-weight:700;"><span style="display:inline-flex; align-items:center; gap:0.35rem;"><i class="fas fa-crown" style="color:#fbbf24;"></i> Admin Panel</span></a>
          <% } %>

          <div class="nav-section-divider desktop-hidden">AKUN SAYA</div>
          <a href="/dashboard?ref_=nav_dashboard" class="nav-link"><span><i class="fas fa-chart-line"></i> Dashboard</span></a>
          <a href="/profile?ref_=nav_profile" class="nav-link desktop-hidden"><span><i class="fas fa-user"></i> Profil Saya</span></a>
          <a href="/wallet?ref_=nav_wallet" class="nav-link desktop-hidden"><span><i class="fas fa-money-bill"></i> Saldo &amp; Top-up</span></a>

          <!-- Desktop User Menu Dropdown -->
          <div class="nav-user-menu mobile-hidden">
            <button class="nav-user-btn" onclick="toggleUserMenu()">
              <% if (user.avatar) { %>
                <img src="<%= user.avatar %>" class="nav-avatar" alt="<%= user.name %>" width="28" height="28">
              <% } else { %>
                <div class="nav-avatar-placeholder"><%= user.name.charAt(0).toUpperCase() %></div>
              <% } %>
              <span style="display:inline-flex; align-items:center; gap:0.25rem;">
                <%= user.name.split(' ')[0] %>
                <% if (user.role === 'admin') { %>
                  <i class="fas fa-crown" style="color:#fbbf24; font-size:0.85rem;" title="Administrator"></i>
                <% } else if (user.isRoyal) { %>
                  <i class="fas fa-crown" style="color:#fbbf24; font-size:0.85rem;" title="Royal Club Member"></i>
                <% } %>
              </span>
              <i class="fas fa-chevron-down"></i>
            </button>
            <div class="user-dropdown" id="userDropdown">
              <% if (user.role === 'admin') { %>
                <a href="/admin?ref_=nav_user_admin" style="color:#fbbf24; font-weight:700;"><i class="fas fa-shield-alt" style="color:#fbbf24;"></i> Admin Panel</a>
                <hr>
              <% } %>
              <a href="/profile?ref_=nav_user_profile"><i class="fas fa-user"></i> Profil</a>
              <a href="/dashboard?ref_=nav_user_dashboard"><i class="fas fa-chart-line"></i> Dashboard</a>
              <a href="/wallet?ref_=nav_user_wallet"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="vertical-align: text-bottom; margin-right: 0.2rem;"><path d="M19 7V4a1 1 0 0 0-1-1H5a2 2 0 0 0 0 4h15a1 1 0 0 1 1 1v4h-3a2 2 0 0 0 0 4h3a8 8 0 0 1-5.3 7.7 2 2 0 0 1-2.7-1.4l-1-4.2a2 2 0 0 0-1.7-1.5L5 16a2 2 0 0 1-2-2V7a2 2 0 0 1 2-2h14"></path><path d="M21 11v4h-3a2 2 0 0 1 0-4h3z"></path></svg> Saldo &amp; Top-up</a>
              <hr>
              <a href="/logout" class="logout-link"><i class="fas fa-sign-out-alt"></i> Logout</a>
            </div>
          </div>
        <% } else { %>
          <a href="/login?ref_=nav_login" class="nav-link"><span><i class="fas fa-sign-in-alt"></i> Login</span></a>
          <a href="/register?ref_=nav_register" class="btn-nav-cta"><i class="fas fa-user-plus"></i> Daftar Gratis</a>
        <% } %>
      </div>

      <div style="display:flex; align-items:center; gap:0.45rem; margin-left:auto; position:relative; z-index:10006;" class="desktop-hidden">
        <!-- Notification Bell on Mobile Top Bar -->
        <button type="button" class="nav-notif-btn" id="notifBellBtn" onclick="toggleNotifMenu()" aria-label="Notifikasi & Promo" style="background:#181920; border:1px solid rgba(255,255,255,0.12); color:#fff; width:38px; height:38px; border-radius:10px; display:inline-flex; align-items:center; justify-content:center; cursor:pointer; position:relative;">
          <i class="fas fa-bell" style="color:#fbbf24; font-size:1.05rem;"></i>
        </button>
        <button class="nav-toggle" type="button" aria-label="Buka menu navigasi" aria-expanded="false" aria-controls="navLinks" id="navToggleBtn" style="background:#181920; border:1px solid rgba(255,255,255,0.12); border-radius:10px; width:38px; height:38px; display:inline-flex; flex-direction:column; align-items:center; justify-content:center; gap:5px; cursor:pointer; padding:0;">
          <span style="display:block; width:18px; height:2px; background:#ededf0; border-radius:2px; transition:0.2s;"></span>
          <span style="display:block; width:18px; height:2px; background:#ededf0; border-radius:2px; transition:0.2s;"></span>
          <span style="display:block; width:18px; height:2px; background:#ededf0; border-radius:2px; transition:0.2s;"></span>
        </button>
      </div>
    </div>
  </nav>


<style>
/* ══════════════════════════════════════════════════════════════════════
   MOBILE NAVIGATION DRAWER - EXACT MATCH TO SCREENSHOT (COMPACT EDITION)
   Strictly scoped to <= 900px. DESKTOP IS 100% UNTOUCHED!
   ══════════════════════════════════════════════════════════════════════ */
@media (max-width: 900px) {
  .nav-links {
    display: none;
    position: fixed !important;
    top: 58px !important;
    left: 0 !important;
    right: 0 !important;
    bottom: 0 !important;
    width: 100% !important;
    max-width: 100% !important;
    box-sizing: border-box !important;
    height: calc(100dvh - 58px) !important;
    max-height: calc(100dvh - 58px) !important;
    background: rgba(13, 15, 20, 0.98) !important;
    backdrop-filter: blur(20px) !important;
    -webkit-backdrop-filter: blur(20px) !important;
    padding: 0.75rem 0.85rem 3.5rem !important;
    flex-direction: column !important;
    flex-wrap: nowrap !important; /* NO 2nd column wrapping! */
    gap: 0 !important;
    z-index: 10005 !important;
    overflow-x: hidden !important; /* NO horizontal overflow or peeking cards! */
    overflow-y: auto !important;
    -webkit-overflow-scrolling: touch !important;
    border-top: 1px solid rgba(255, 255, 255, 0.08) !important;
  }
  .nav-links.open {
    display: flex !important;
  }
  
  /* Strictly hide desktop user dropdown menu on mobile drawer */
  .nav-links .nav-user-menu,
  .nav-links .nav-user-menu.mobile-hidden {
    display: none !important;
  }
  .nav-toggle.open span:nth-child(1) {
    transform: rotate(45deg) translate(5px, 5px) !important;
  }
  .nav-toggle.open span:nth-child(2) {
    opacity: 0 !important;
  }
  .nav-toggle.open span:nth-child(3) {
    transform: rotate(-45deg) translate(5px, -5px) !important;
  }

  /* 1. Mobile Profile Card (Compact) */
  .mobile-user-card {
    background: #181920 !important;
    border: 1px solid rgba(255, 255, 255, 0.08) !important;
    border-radius: 14px !important;
    padding: 0.65rem 0.85rem !important;
    margin-bottom: 0.45rem !important;
    display: flex !important;
    align-items: center !important;
    justify-content: space-between !important;
    width: 100% !important;
    box-sizing: border-box !important;
  }
  .mobile-user-info {
    display: flex !important;
    align-items: center !important;
    gap: 0.6rem !important;
  }
  .mobile-avatar-placeholder {
    width: 38px !important;
    height: 38px !important;
    border-radius: 50% !important;
    background: #8b5cf6 !important; /* Purple circle */
    border: 2px solid #fbbf24 !important;
    color: #ffffff !important;
    font-weight: 800 !important;
    font-size: 1.1rem !important;
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
  }
  .mobile-user-name {
    font-family: var(--font-display, sans-serif) !important;
    font-weight: 800 !important;
    font-size: 0.95rem !important;
    color: #ffffff !important;
    line-height: 1.2 !important;
  }
  .mobile-user-balance {
    font-family: var(--font-ui, sans-serif) !important;
    font-size: 0.8rem !important;
    color: #fbbf24 !important;
    font-weight: 700 !important;
    margin-top: 1px !important;
  }
  .mobile-user-actions {
    display: flex !important;
    flex-direction: column !important;
    align-items: flex-end !important;
    gap: 0.25rem !important;
  }
  .mobile-user-btn-row {
    display: flex !important;
    align-items: center !important;
    gap: 0.3rem !important;
  }
  .btn-mobile-admin,
  .btn-mobile-topup {
    background: #f59e0b !important;
    color: #000000 !important;
    font-weight: 800 !important;
    font-size: 0.72rem !important;
    padding: 0.25rem 0.55rem !important;
    border-radius: 7px !important;
    text-decoration: none !important;
    display: inline-flex !important;
    align-items: center !important;
    gap: 0.2rem !important;
    line-height: 1.2 !important;
  }
  .btn-mobile-logout {
    background: rgba(239, 68, 68, 0.2) !important;
    border: 1px solid rgba(239, 68, 68, 0.4) !important;
    color: #fca5a5 !important;
    font-size: 0.68rem !important;
    font-weight: 700 !important;
    padding: 0.15rem 0.5rem !important;
    border-radius: 5px !important;
    text-decoration: none !important;
    line-height: 1.2 !important;
  }

  /* 2. Menu Items (Separate Compact Card Boxes) */
  .nav-links .nav-link {
    background: #181920 !important;
    border: 1px solid rgba(255, 255, 255, 0.05) !important;
    border-radius: 11px !important;
    padding: 0.5rem 0.9rem !important;
    margin-bottom: 0.32rem !important;
    display: flex !important;
    align-items: center !important;
    justify-content: space-between !important;
    width: 100% !important;
    box-sizing: border-box !important;
    color: #f1f5f9 !important;
    font-size: 0.88rem !important;
    font-weight: 600 !important;
    text-decoration: none !important;
    transition: all 0.15s ease !important;
    min-height: 40px !important;
  }
  .nav-links .nav-link:active {
    background: #20222a !important;
    transform: scale(0.99) !important;
  }
  .nav-links .nav-link span {
    display: flex !important;
    align-items: center !important;
    gap: 0.75rem !important;
  }
  .nav-links .nav-link i {
    color: #fbbf24 !important; /* Gold icons */
    font-size: 1rem !important;
    width: 18px !important;
    text-align: center !important;
  }

  /* 3. Badge "100+ GAME" */
  .nav-links .badge-hot {
    background: linear-gradient(135deg, #ef4444 0%, #f97316 100%) !important;
    color: #ffffff !important;
    font-size: 9px !important;
    font-weight: 800 !important;
    padding: 0.2rem 0.5rem !important;
    border-radius: 9999px !important;
    letter-spacing: 0.04em !important;
    border: none !important;
    box-shadow: none !important;
    animation: none !important;
  }

  /* 4. Section Dividers (Compact) */
  .nav-section-divider {
    text-align: center !important;
    font-size: 0.68rem !important;
    font-weight: 800 !important;
    letter-spacing: 0.18em !important;
    color: #94a3b8 !important;
    margin: 0.65rem 0 0.3rem !important;
    text-transform: uppercase !important;
    width: 100% !important;
  }
  .nav-admin-divider {
    color: #f59e0b !important;
  }

  /* 5. Special Admin Panel Card (Compact) */
  .nav-links .nav-admin-card {
    border: 1.5px solid #eab308 !important; /* Gold border */
    justify-content: center !important;
    text-align: center !important;
    padding: 0.52rem 0.9rem !important;
    min-height: 40px !important;
  }
  .nav-links .nav-admin-card span {
    color: #fbbf24 !important;
    font-weight: 800 !important;
    font-size: 0.95rem !important;
    justify-content: center !important;
  }
}
</style>


<!-- Pusat Notifikasi & Promo Drawer / Modal Popup -->
<style>
.notif-dropdown-box {
  display: none;
  position: fixed;
  top: 60px;
  right: 8px;
  width: 320px;
  max-width: calc(100vw - 16px);
  max-height: 440px;
  overflow-y: auto;
  background: #11131f;
  border: 1px solid rgba(255, 255, 255, 0.18);
  border-radius: 16px;
  box-shadow: 0 20px 50px rgba(0, 0, 0, 0.95);
  z-index: 99999999 !important;
}
.notif-backdrop-overlay {
  display: none;
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 99999998 !important;
}
</style>

<div class="notif-backdrop-overlay" id="notifBackdrop" onclick="toggleNotifMenu()"></div>

<div class="notif-dropdown-box" id="notifDropdown">
  <div style="display:flex; align-items:center; justify-content:space-between; padding:0.85rem 1rem; background:rgba(255,255,255,0.04); border-bottom:1px solid rgba(255,255,255,0.08);">
    <div style="display:flex; align-items:center; gap:0.5rem;">
      <i class="fas fa-bell" style="color:#fbbf24;"></i>
      <span style="font-weight:800; font-size:0.9rem; color:#fff;">Pusat Notifikasi &amp; Promo</span>
    </div>
    <button type="button" onclick="toggleNotifMenu()" style="background:none; border:none; color:rgba(255,255,255,0.6); font-size:1.1rem; cursor:pointer;">&times;</button>
  </div>

  <div style="max-height:360px; overflow-y:auto; padding:0.75rem;">
    <!-- Promo Box -->
    <div style="background:linear-gradient(135deg, rgba(251,191,36,0.15) 0%, rgba(217,119,6,0.08) 100%); border:1px solid rgba(251,191,36,0.3); border-radius:12px; padding:0.85rem; margin-bottom:0.75rem;">
      <div style="font-weight:800; font-size:0.82rem; color:#fbbf24; margin-bottom:0.35rem; display:flex; align-items:center; gap:0.35rem;">
        <i class="fas fa-gift"></i> Kode Promo Spesial 10%
      </div>
      <div style="font-size:0.8rem; color:rgba(255,255,255,0.9); line-height:1.4; margin-bottom:0.6rem;">
        Pakai kode promo <strong style="color:#fbbf24;">ALEX10</strong> saat pembelian paket/top-up saldo di atas Rp100.000 untuk potongan harga 10%!
      </div>
      <button type="button" onclick="copyPromoCode('ALEX10')" style="background:linear-gradient(135deg, #fbbf24 0%, #d97706 100%); color:#000; border:none; border-radius:8px; padding:0.35rem 0.75rem; font-size:0.75rem; font-weight:800; cursor:pointer; display:inline-flex; align-items:center; gap:0.35rem; width:100%; justify-content:center;">
        <i class="fas fa-copy"></i> Salin Kode: ALEX10
      </button>
    </div>

    <!-- Feature Update Notification -->
    <div style="background:rgba(255,255,255,0.04); border:1px solid rgba(255,255,255,0.08); border-radius:12px; padding:0.75rem; margin-bottom:0.6rem; display:flex; gap:0.6rem;">
      <div style="width:32px; height:32px; border-radius:8px; background:rgba(59,130,246,0.2); color:#60a5fa; display:flex; align-items:center; justify-content:center; flex-shrink:0;">
        <i class="fas fa-gamepad"></i>
      </div>
      <div>
        <div style="font-weight:800; font-size:0.8rem; color:#fff; margin-bottom:0.2rem;">EA FC 26 &amp; GTA V Ready</div>
        <div style="font-size:0.75rem; color:rgba(255,255,255,0.7); line-height:1.35;">Sewa Cloud Gaming RTX - Main EA FC 26 &amp; GTA V Expanded langsung dari HP &amp; PC tanpa install!</div>
      </div>
    </div>

    <!-- Payment Notification -->
    <div style="background:rgba(255,255,255,0.04); border:1px solid rgba(255,255,255,0.08); border-radius:12px; padding:0.75rem; display:flex; gap:0.6rem;">
      <div style="width:32px; height:32px; border-radius:8px; background:rgba(16,185,129,0.2); color:#34d399; display:flex; align-items:center; justify-content:center; flex-shrink:0;">
        <i class="fas fa-shield-alt"></i>
      </div>
      <div>
        <div style="font-weight:800; font-size:0.8rem; color:#fff; margin-bottom:0.2rem;">Pembayaran QRIS Instan 24/7</div>
        <div style="font-size:0.75rem; color:rgba(255,255,255,0.7); line-height:1.35;">Metode pembayaran QRIS (All E-Wallet &amp; m-Banking) terkonfirmasi otomatis secara realtime 24 jam nonstop.</div>
      </div>
    </div>
  </div>
</div>

<script>
function toggleNotifMenu() {
  var el = document.getElementById('notifDropdown');
  var bd = document.getElementById('notifBackdrop');
  if (!el) return;
  var isOpen = el.style.display === 'block';
  el.style.display = isOpen ? 'none' : 'block';
  if (bd) bd.style.display = isOpen ? 'none' : 'block';
}

function copyPromoCode(code) {
  if (navigator.clipboard && navigator.clipboard.writeText) {
    navigator.clipboard.writeText(code).then(function() {
      alert('✅ Kode Promo ' + code + ' Berhasil Disalin!');
    }).catch(function() {
      alert('Kode Promo: ' + code);
    });
  } else {
    alert('Kode Promo: ' + code);
  }
}
</script>

